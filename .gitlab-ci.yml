image: docker:latest

services:
  - docker:dind

variables:
  IMAGE: ${IMAGE_BASE_NAME}
  DOCKER_PASSWORD: ${DOCKER_PASSWORD}
  DOCKER_USER: ${DOCKER_USER}
  REGISTRY: ${REGISTRY}
 
stages:
  - prepare
  - build

check_deps:
  stage: prepare
  script:
    - docker --version
build:
  stage: build
  script:
      - echo -n "${DOCKER_PASSWORD}" | docker login -u ${DOCKER_USER} --password-stdin ${REGISTRY}
      - docker build -t ${IMAGE}:${CI_COMMIT_SHORT_SHA} -f ./Dockerfile .
      - docker push ${IMAGE}
      - docker logout
  only:
    - master
    - add-docker-to-ci
